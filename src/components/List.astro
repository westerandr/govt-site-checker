---
  import sitesData from '../assets/sites.json'
  import ListItem from './ListItem.astro'
---
<ul role="list" class="divide-y divide-gray-100 w-7/12 mx-auto grid grid-cols-2 gap-x-11">
  {sitesData.sites.map((site) => (<ListItem title={site.name} url={site.site} />))}
</ul>